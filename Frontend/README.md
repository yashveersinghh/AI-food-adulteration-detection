## FoodGuard AI (Frontend)

This is a standard **Vite + React + TypeScript** frontend.

### Run locally (Windows / PowerShell)

```powershell
npm install
npm run dev
```

Vite will start on `http://localhost:5173`.

